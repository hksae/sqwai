## TUI frames and Ctrl+V follow-up
- Hardened expanded tool/diff frame layout for narrow terminals by removing the artificial minimum inner width; complete frame rows now fit the current chat width with both borders preserved.
- Kept display-width-aware truncation/padding for Unicode and long lines.
- Added a paste Enter guard for Windows terminal sequences so Ctrl+V cannot consume a following Enter as message submission.
- Added regression coverage for narrow frame geometry and Ctrl+V behavior.
- Verification: `cargo test -q` passed (114 passed, 0 failed, 1 ignored). Checkpoint commit: `736b775 Harden resizable TUI frames and paste handling`.
- Fixed the remaining code-block case: long highlighted source lines are now wrapped inside the code frame, with display-width-aware sizing and borders kept on every physical row. Added a narrow C++ rendering assertion. Verification: `cargo test -q` passed (115 passed, 0 failed, 1 ignored). Commit: `d334b47 Wrap long code lines inside frames`.
- Continued Phase 5 without reflect: hardened Git safety so commits and branch create/switch get checkpoints, while git_branch inspection remains available in Plan mode and mutation is gated per action. Verification: `cargo test -q` passed (115 passed, 0 failed, 1 ignored). Commit: `790e07e Harden Git tool mode safety`.
- Added the first Phase 5 web capability without reflect: bounded async `webfetch` with HTTP(S)-only URL validation, timeout and redirect limits, 1 MB response cap, 40k-character output cap, HTML text extraction, tool schema/summary, and async agent-loop dispatch. Verification: `cargo test -q` passed (115 passed, 0 failed, 1 ignored). Commit: `36f5a9e Add bounded webfetch tool`.
- Added `websearch` without reflect: bounded DuckDuckGo HTML search with query/count/timeout validation, normalized title/URL/snippet extraction, response limits, tool schema/summary, and async dispatch. Verification: `cargo test -q` passed (115 passed, 0 failed, 1 ignored). Commit: `8ae2416 Add websearch tool`.
- Fixed a webfetch startup panic caused by unsupported regex backreferences; replaced it with a Rust-regex-compatible pattern. Also fixed long user prompts by wrapping them before adding their frame, preserving both borders. Verification: `cargo test -q` passed (116 passed, 0 failed, 1 ignored). Commit: `48ab3c1 Fix webfetch regex and long prompt frames`.
- Added mouse-wheel scrolling for multi-line input and stopped retrying deterministic provider 400/incompatibility errors such as function tools plus reasoning_effort. Verification: `cargo test -q` passed (116 passed, 0 failed, 1 ignored). Commits: `4b13260`, `0da3faa`.
- Corrected input wheel hit-testing: it now uses the actual rendered input rectangle rather than the chat rectangle, so wheel events over a multiline prompt scroll the input. Verification: `cargo test -q` passed (116 passed, 0 failed, 1 ignored). Commit: `705e006 Scroll multiline input with mouse wheel`.
- Added mouse-drag selection for text in the multiline input using tui-textarea selection APIs; releasing the drag copies the selected text to the system clipboard and cancels the internal selection state. Verification: `cargo test -q` passed (116 passed, 0 failed, 1 ignored). Commit: `a8758d2 Select input text with mouse drag`.
- Removed all bottom menu footer hints from rendering, including enter/esc/navigation/save/cancel instructions; menu behavior remains unchanged. Verification: `cargo test -q` passed (116 passed, 0 failed, 1 ignored). Commit: `e080101 Remove menu footer hints`.
- Updated sessions UI: pinned section now uses a normal solid rounded frame instead of a dashed header; opening sessions parses only a bounded recent visible window (40 metadata/session records), avoiding full history loading for every saved session. Verification: `cargo test -q` passed (116 passed, 0 failed, 1 ignored). Commit: `d6529d2 Speed up sessions menu loading`.
- Fixed busy feedback and input selection: all busy paths now use one transient `busy · esc to stop` notice, deduplicate it, expire it after 2 seconds, clear it on stop/completion, and preserve text typed while streaming. Input mouse selection now uses the active theme and remains highlighted after copy. Verification: `cargo test -q` passed (117 passed, 0 failed, 1 ignored).
- Linked the bottom `th:` menu options to model thinking settings by making both use the same complete five-level list, including `off`; selecting any level updates the active model setting that the status bar displays. Verification: `cargo test -q` passed (118 passed, 0 failed, 1 ignored).
- Started Phase 5 subagent infrastructure without reflection: added a read-only `subagent` tool, bounded child depth, child lifecycle/progress events, and expandable transcript rows with task/status/output tracking. Verification: `cargo test -q` passed (118 passed, 0 failed, 1 ignored). The first iteration still needs a dedicated subagent overview menu and focused integration tests before being considered complete. Commit: `d7727e0`.
- Added `AgentHandle` drop cancellation so child agent tasks cannot continue after their handle is discarded. Verification: `cargo test -q` passed (118 passed, 0 failed, 1 ignored). Commit: `1383c98`.
- Added the subagent overview UI: `Ctrl+A` opens a list of child agents with state/task, Enter opens an individual detail view with activity/result, and transcript rows remain independently expandable. Added menu regression coverage. Verification: `cargo test -q` passed (119 passed, 0 failed, 1 ignored). Commit: `4e47a91`.
- Added bounded parallel subagent execution: one tool call accepts either one task or up to 8 tasks, runs at most 4 children concurrently, emits independent lifecycle events, and returns results in original task order. Added validation tests for single/multiple tasks and the hard limit. Verification: `cargo test -q` passed (121 passed, 0 failed, 1 ignored). Commit: `80831a4`.
- Added live subagent lifecycle UI: a clickable status-bar summary shows running/total or failure counts, open subagent menus refresh as events arrive, and Esc marks unfinished children `cancelled` while preserving completed states. Verification: `cargo test -q` passed (122 passed, 0 failed, 1 ignored). Commit: `e227219`.
- Simplified collapsed subagent transcript rows to show only `subagent-N` plus a status marker; delegated task text is hidden from the main chat until the subagent UI is redesigned. Verification: `cargo test -q` passed (122 passed, 0 failed, 1 ignored). Commit: `55c0cf8`.
- Fixed stop cleanup and active model edits: Esc now removes all transient subagent rows, the parent `subagent` tool row, and the `agents:` indicator; saving edits to the active model immediately refreshes `model_cfg`, context limit, provider, and continuation state, so `th:` changes apply without reselecting the model. Verification: `cargo test -q` passed (122 passed, 0 failed, 1 ignored). Commit: `ae2d870`.
- Replaced subagent log/details with a full read-only chat view. Clicking `subagent-N` or choosing it through Ctrl+A opens the same screen; the delegated task renders as a user message, child thinking/tools/results/final answer use normal transcript segments, the input/status controls are absent, and only `esc close` remains. Verification: `cargo test -q` passed (122 passed, 0 failed, 1 ignored). Commit: `e63b882`.
- Fixed subagent chat click routing: clicks now resolve segment indices against the active child's transcript rather than the hidden main transcript, so finished child tool rows expand/collapse and cannot accidentally switch to another subagent. Added a regression test. Verification: `cargo test -q` passed (123 passed, 0 failed, 1 ignored). Commit: `bbe2511`.
- Made `white` the first theme and therefore the default for fresh configurations, moving its existing palette entry to the top of the themes menu. Added a regression test. Verification: `cargo test -q` passed (124 passed, 0 failed, 1 ignored). Commit: `3f87da7`.
- Added explicit subagent guidance to `src/prompts/system.md`: use children for materially useful independent parallel investigation, work directly for trivial/sequential/mutating tasks, keep children read-only, enforce 8-task/4-concurrent/depth-1 limits, verify child findings, and keep reflector work postponed. Verification: `cargo test` passed (124 passed, 0 failed, 1 ignored); `cargo build --release` passed with existing dead-code warnings.
- Began final Phase 5 stabilization. Updated the ignored design document locally to reflect `subagent`, current limits/UI, white default, and postponed reflection. Added four web helper/parser regression tests and hardened host validation. Web tests pass (4/4). Committed tracked changes: `43cfdfb` and `3fc2a51`.
- Documented the chosen subagent execution policy in `DESIGN.md`: the delegation interface is controlled by the parent, while children inherit the user's Plan/Act mode; Plan children are read-only, Act children may modify under checkpoint/undo protection. Also recorded the 8/4 limits and no nesting. Committed as `29bab4c`.
- Updated `src/prompts/system.md` and the `subagent` tool description to remove the unconditional read-only wording and describe inherited Plan/Act permissions. Kept the registry classification as ReadOnly because it controls Plan-mode schema filtering, not the child's inherited runtime mode. Updated `DESIGN.md` wording accordingly. Tests for subagent/schema passed. Commits: `699f13d`, `0e09829`.
- Created a detailed 790-line `GRAPH.md` covering the Phase 6 embedded graph database, universal adapters, storage model, GraphStore API, indexing lifecycle, agent operations, terminal graph-view, testing, implementation plan, and deferred native/web surfaces. Added `/GRAPH.md` to `.gitignore` and committed that rule as `31787f4`; the design document remains intentionally local and ignored.
- Implemented the first Phase 6 graph foundation behind `GraphStore`: CozoDB 0.7.6 with bundled SQLite and Rayon, project-local `.sqwai/graph/graph.db`, universal nodes/edges, schema initialization/version validation, idempotent upserts, transactional batch writes with rollback, bounded directional BFS projections, persistence, and contract tests. Updated `GRAPH.md` with the engine decision and risks. Full verification: 133 passed, 0 failed, 1 ignored; `cargo build --release` passed. A compiler compatibility fix in `src/tui/app/menus.rs` replaced `String + &String` with `format!`; existing web test formatting changes were retained.
- Added Phase 6.2 `graph_index.rs`: generic file nodes, Markdown document/heading/link extraction, SHA-256 hashes, bounded reads, Git ignore support without requiring a Git repository, binary/size filtering, and transactional per-file subgraph replacement. Verification before commit: 137 passed, 0 failed, 1 ignored; `cargo build --release` passed. GRAPH.md now marks the initial 6.2 implementation complete while full stale-file cleanup remains pending.
- Completed Phase 6.2 graph index synchronization: added `GraphStore::prune_file_subgraphs`, a shared `remove_file_subgraph` helper, full-project stale-file cleanup after a successful walk (`removed_files` in `IndexReport`), and a public synchronous `/graph-rebuild` TUI command with streaming guard. Added a regression test for deleted-file subgraph removal and a COMMAND-list assertion. Updated GRAPH.md to mark stale cleanup and the command complete. Verification: 138 passed, 0 failed, 1 ignored; `cargo fmt --check` clean; `git diff --check` clean; `cargo build --release` passed. Commit: `Complete graph index synchronization`.
- Paused graph development (Phase 6). Rewrote `DESIGN.md` §5 into a memory/plan/journal architecture: hard `plan` tool with operations (create/start/finish/cancel/add/split/block), no `set_goal`, code-only validator, `journal/<session>.jsonl` event log written by code (distinct from the existing undo checkpoint journal in §6), `memory/YYYY-MM-DD.md` diary driven by a host fact block with append-only + compaction/step-close/exit triggers, curated `MEMORY.md` via `memory_propose`, and session bootstrap priority. Also updated §1/§2/§4.1/§7/§9/§12 for consistency and fixed the stale `.sqwai/graph.json` references to `.sqwai/graph/` (CozoDB). Commit: `a9e6d6a`.
- Refreshed `DESIGN.md` again per user request to remove stale content: rewrote §10 TUI (dropped the old ascii mock, current commands incl. /graph-rebuild, /verify; indicator `[verified]`), replaced §4.6 with the full Reflector spec (L0/L1/L2, neutralizer/executor/verdict, config, self-protection, metrics), reordered §12 phases so memory+journal (phase 6) and Reflector (phase 7) come before graph continuation (phase 8), updated §4.1/§4.5/§7 status/§13/§14 (resolved reflect + memory-format open questions). Commits: `f05164c`.
- User will rewrite `DESIGN.md` fundamentally (direction may shift). Removed `/DESIGN.md` from `.gitignore` (commit `c2005d5`); `DESIGN.md` was already force-tracked earlier, so it stays versioned and any rewrite lands as a normal diff. `GRAPH.md` deleted earlier (consolidated into DESIGN.md). Hold new implementation until the new DESIGN.md lands.
- Incorporated the user's external-risk + enhancement pass into `DESIGN.md` (design still marked not-final). Added: §2.0 single-instance lock + USER.md/USER-wide split; §2.1 cmd:/manual: acceptance runners, open-assumption finish warning, §2.1.8 scope guard + plan-first gate; §2.2 journal kinds external_change/claim_lint, trust:low field, assumption/lesson note kinds, untrusted-input rule; §2.3 memory_propose scope; §2.4 freshness external_change trigger + lessons in context block; §3.2 external/scope block-D lines; §3.3 staged compaction + files-read/open-assumptions/external in anchor; §3.7 cancel-mid-tool + provider-fallback rows; new §3.8 claim lint; §5.1 provider fallback; §5.2 shell-aware PowerShell safety; §5.4 /why /export /bench + step-diff panel; §6 untrusted-content rule; §7 work-queue items L–Z; §9 sandbox/open-question additions; new §11 explicitly-excluded. File now 1312 lines.
- Committed the revised `DESIGN.md` as `9e94503` (`Revise DESIGN.md: hard plan, journal, diary, Reflector, and external-risk hardening`). Only DESIGN.md was included; working tree is clean.
- Закреплено проектное правило: коммитить каждое малое, промежуточное и незавершённое изменение отдельным коммитом; пункт L начинается первым.
